name: Build Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

# 同一分支只保留一条最新流水线：避免连续合并时多个 build 并发竞争覆盖 latest 标签。
concurrency:
  group: docker-image-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/baoxinwen/ledger

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    # 显式超时：默认 360 分钟上限内挂起的 job 会持续占用 runner。
    timeout-minutes: 20
    # 测试任务只需要读代码权限，不授予任何写权限（最小权限原则）。
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install and test server
        working-directory: server
        run: |
          npm ci
          npm test

      - name: Install, lint, test and build client
        working-directory: client
        run: |
          npm ci
          npm run lint
          npm test
          npm run build

  e2e:
    name: Run e2e tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Install client dependencies and Playwright browser
        working-directory: client
        run: |
          npm ci
          npx playwright install --with-deps chromium

      # e2e 自包含：Playwright 配置会自动拉起隔离后端（临时数据库 + 已知初始化 Token）与前端。
      - name: Run Playwright e2e
        working-directory: client
        run: npm run test:e2e

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: client/playwright-report/
          retention-days: 7

  build:
    name: Build and publish
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [test, e2e]
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
