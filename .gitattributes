# 行尾治理：仓库内统一 LF，避免 Windows 上 CRLF 往返改写产生伪 diff。
* text=auto

# 脚本必须保持 LF（docker-entrypoint.sh 在 Linux 容器内执行）。
*.sh text eol=lf

# 二进制资源不做行尾处理。
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.db binary
